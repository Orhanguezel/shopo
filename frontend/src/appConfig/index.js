const appConfig = {
  BASE_URL: process.env.NEXT_PUBLIC_BASE_URL || 'https://admin.seyfibaba.com/',
  PWA_STATUS: process.env.NEXT_PWA_STATUS || '0',
  APPLICATION_URL: process.env.NEXT_APPLICATION_URL || 'https://seyfibaba.com',
};

export default appConfig;
